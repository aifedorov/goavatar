apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: goavatar-server
  namespace: goavatar
spec:
  selector:
    matchLabels:
      app: goavatar-server
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
