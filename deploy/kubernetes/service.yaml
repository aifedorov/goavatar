apiVersion: v1
kind: Service
metadata:
  name: goavatar-server
  namespace: goavatar
  labels:
    app: goavatar-server
spec:
  type: ClusterIP
  selector:
    app: goavatar-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
